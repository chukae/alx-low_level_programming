#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - Reurns the length of a string.
 *  @s: string
 *
 * Return: returns length of string as int.
 **/
int _strlen(char *s)
{
	int val = 0;
	val = strlen(s);

	return (val);
}

