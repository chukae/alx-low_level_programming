#include <stdio.h>
#include "main.h"

/**
 * _puts - function that prints a string, followed by a new line, to stdout.
 *
 *@str: pointer variable to be reset
 *
 * Return: Always void.
 */
void _puts(char *str)
{
	puts(str);
}
